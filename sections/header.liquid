<header class="site-header{% if section.settings.sticky_header %} site-header--sticky{% endif %}" data-site-header>
  <div class="site-header__inner page-width">

    <button class="site-header__mobile-toggle" data-mobile-nav-toggle aria-label="{{ 'sections.header.menu' | t }}" aria-expanded="false" aria-controls="MobileNav">
      {% render 'icon-menu' %}
    </button>

    <div class="site-header__logo">
      <a href="{{ routes.root_url }}" class="site-header__logo-link">
        {%- if settings.logo != blank -%}
          {{ settings.logo | image_url: width: settings.logo_width | image_tag:
            loading: 'eager',
            alt: shop.name,
            width: settings.logo_width,
            class: 'header__logo-img'
          }}
        {%- else -%}
          <svg class="logo-pagoda-svg" viewBox="0 0 120 80" fill="none" xmlns="http://www.w3.org/2000/svg">
            <line class="accent-stroke" x1="60" y1="5" x2="60" y2="25" stroke="var(--color-accent)" stroke-width="2" stroke-linecap="round"/>
            <circle cx="60" cy="9" r="1.5" fill="var(--color-accent)"/>
            <circle cx="60" cy="13" r="2" fill="var(--color-accent)"/>
            <circle cx="60" cy="17" r="2.5" fill="var(--color-accent)"/>
            <circle cx="60" cy="21" r="3" fill="var(--color-accent)"/>
            <path class="bg-fill" d="M52 26C55 26 58 24 60 24C62 24 65 26 68 26" stroke="#111111" stroke-width="2" stroke-linecap="round"/>
            <path class="bg-fill" d="M55 26.5L55 31H65L65 26.5" stroke="#111111" stroke-width="1.5"/>
            <path class="bg-fill" d="M40 40C48 40 55 35 60 35C65 35 72 40 80 40C73 35 68 34 60 34C52 34 47 35 40 40Z" fill="#111111"/>
            <path class="accent-stroke" d="M42 40.5C50 40.5 56 39 60 39C64 39 70 40.5 78 40.5" stroke="var(--color-accent)" stroke-width="1.2" fill="none"/>
            <rect class="bg-fill" x="46" y="40" width="28" height="6" fill="#111111"/>
            <rect class="window-fill" x="50" y="41" width="3" height="5" fill="#FFFFFF"/>
            <rect class="window-fill" x="58" y="41" width="4" height="5" fill="#FFFFFF"/>
            <rect class="window-fill" x="67" y="41" width="3" height="5" fill="#FFFFFF"/>
            <path class="bg-fill" d="M28 54C40 54 52 48 60 48C68 48 80 54 92 54C83 48 72 47 60 47C48 47 37 48 28 54Z" fill="#111111"/>
            <path class="accent-stroke" d="M30 54.5C42 54.5 52 52.5 60 52.5C68 52.5 78 54.5 90 54.5" stroke="var(--color-accent)" stroke-width="1.8" fill="none"/>
          </svg>
          <span class="site-header__logo-text">
            <span class="logo-tokiyo">T<span class="red-o">O</span>KIY<span class="red-o">O</span></span>
            <span class="logo-lifestyle">LIFESTYLE</span>
          </span>
        {%- endif -%}
      </a>
    </div>

    <nav class="site-header__nav" aria-label="Primary">
      <ul class="site-header__nav-list" role="list">
        {%- for link in section.settings.main_menu.links -%}
          <li class="site-header__nav-item{% if link.links.size > 0 %} has-dropdown{% endif %}">
            <a href="{{ link.url }}" class="site-header__nav-link" {% if link.links.size > 0 %}aria-haspopup="true" aria-expanded="false" data-mega-menu-trigger="{{ link.handle }}"{% endif %}>
              {{ link.title }}
              {%- if link.links.size > 0 -%}{% render 'icon-chevron-down' %}{%- endif -%}
            </a>
            {%- if link.links.size > 0 -%}
              {% render 'mega-menu', link: link %}
            {%- endif -%}
          </li>
        {%- endfor -%}
      </ul>
    </nav>

    <div class="site-header__icons">
      {%- if section.settings.enable_search -%}
        <button class="site-header__icon-btn" data-search-drawer-toggle aria-label="{{ 'sections.header.search' | t }}">
          {% render 'icon-search' %}
        </button>
      {%- endif -%}

      <a href="{{ routes.account_url }}" class="site-header__icon-btn" aria-label="{{ 'sections.header.account' | t }}">
        {% render 'icon-account' %}
      </a>

      {%- if settings.enable_wishlist -%}
        <a href="{{ routes.root_url }}pages/wishlist" class="site-header__icon-btn" data-wishlist-link aria-label="{{ 'sections.header.wishlist' | t }}">
          {% render 'icon-heart' %}
          <span class="site-header__icon-count" data-wishlist-count>0</span>
        </a>
      {%- endif -%}

      <button class="site-header__icon-btn" data-cart-drawer-toggle aria-label="{{ 'sections.header.cart' | t }}">
        {% render 'icon-cart' %}
        <span class="site-header__icon-count" data-cart-count>{{ cart.item_count }}</span>
      </button>
    </div>
  </div>
</header>

<div class="mobile-nav" id="MobileNav" data-mobile-nav aria-hidden="true">
  <div class="mobile-nav__header">
    <span class="mobile-nav__title">{{ shop.name }}</span>
    <button class="mobile-nav__close" data-mobile-nav-close aria-label="{{ 'accessibility.close' | t }}">
      {% render 'icon-close' %}
    </button>
  </div>
  <ul class="mobile-nav__list" role="list">
    {%- for link in section.settings.main_menu.links -%}
      <li class="mobile-nav__item">
        <a href="{{ link.url }}" class="mobile-nav__link">{{ link.title }}</a>
        {%- if link.links.size > 0 -%}
          <button class="mobile-nav__expand" aria-label="Expand {{ link.title }}" data-mobile-submenu-toggle>
            {% render 'icon-chevron-down' %}
          </button>
          <ul class="mobile-nav__sublist" role="list">
            {%- for child_link in link.links -%}
              <li><a href="{{ child_link.url }}">{{ child_link.title }}</a></li>
            {%- endfor -%}
          </ul>
        {%- endif -%}
      </li>
    {%- endfor -%}
  </ul>
</div>
<div class="mobile-nav__overlay" data-mobile-nav-overlay></div>

{% schema %}
{
  "name": "Header",
  "tag": "header",
  "class": "section-header",
  "settings": [
    {
      "type": "link_list",
      "id": "main_menu",
      "label": "Menu",
      "default": "main-menu"
    },
    {
      "type": "checkbox",
      "id": "sticky_header",
      "label": "Enable sticky header",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_search",
      "label": "Enable search icon",
      "default": true
    },
    {
      "type": "header",
      "content": "Mega Menu Feature Image"
    },
    {
      "type": "image_picker",
      "id": "mega_menu_image",
      "label": "Feature image"
    },
    {
      "type": "url",
      "id": "mega_menu_image_link",
      "label": "Feature image link"
    },
    {
      "type": "text",
      "id": "mega_menu_image_caption",
      "label": "Feature image caption",
      "default": "New Season Edit"
    }
  ]
}
{% endschema %}
